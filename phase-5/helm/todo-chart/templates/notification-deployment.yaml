{{- if .Values.notification.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-notification
  labels:
    app: notification
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      app: notification
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: notification
        release: {{ .Release.Name }}
      {{- if .Values.notification.dapr.enabled }}
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: {{ .Values.notification.dapr.appId | quote }}
        dapr.io/app-port: {{ .Values.notification.dapr.appPort | quote }}
        dapr.io/app-protocol: "http"
      {{- end }}
    spec:
      containers:
        - name: notification
          image: "{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag }}"
          imagePullPolicy: {{ .Values.notification.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.notification.service.port }}
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: database_url
          resources:
            {{- toYaml .Values.notification.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.notification.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.notification.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
      restartPolicy: Always
{{- end }}
